/* 
    1- What is LinkedList ?
        ** LinkedList is a linear datastructure includes connected nodes and each node contains value (from any datatype) and pointer (next) points to next node in memory address location.
        ** LinkedList is scatterd memory address location (not continuous memory address location)
        **
*/